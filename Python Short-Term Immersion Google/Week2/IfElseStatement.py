# The is_positive function should return true if the received number is positive, otherwise it returns none. Can you fill in the gaps to do so?

def is_positive(number):
    if number > 0:
        return True

# is_positive function must return true if the received number is positive, otherwise return false. Can you fill in the gaps to do so?


def is_positive(number):
    if number > 0:
        return True
    else:
        return False
